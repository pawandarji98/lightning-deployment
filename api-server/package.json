{
  "name": "api-server",
  "version": "1.0.0",
  "description": "The API server assumes the responsibility of processing user requests, executing Task commands on AWS, meticulously capturing all logs, and seamlessly transmitting them to the Redis server.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Pawan Darji",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-ecs": "^3.511.0",
    "express": "^4.18.2",
    "ioredis": "^5.3.2",
    "random-word-slugs": "^0.1.7",
    "socket.io": "^4.7.4"
  }
}
